[project]
name = "pii-redaction"
version = "0.1.0"
description = "Detect and redact PII before it reaches the model or logs (Module 7 demo)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.0",
]
