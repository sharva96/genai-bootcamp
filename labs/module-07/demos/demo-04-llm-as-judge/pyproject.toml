[project]
name = "llm-as-judge"
version = "0.1.0"
description = "Grade answers for groundedness with a structured-output LLM judge (Module 7 demo)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.2.0",
    "python-dotenv>=1.0.0",
]
