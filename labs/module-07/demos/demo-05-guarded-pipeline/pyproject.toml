[project]
name = "guarded-pipeline"
version = "0.1.0"
description = "All guardrails composed around one LLM call, end to end, with an audit log (Module 7 demo)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.2.0",
    "python-dotenv>=1.0.0",
]
