[project]
name = "vision-multimodal"
version = "0.1.0"
description = "Image understanding with a multimodal chat model (Module 6 demo)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.2.0",
    "python-dotenv>=1.0.0",
]
