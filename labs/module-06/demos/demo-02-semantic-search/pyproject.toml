[project]
name = "semantic-search"
version = "0.1.0"
description = "In-memory semantic search with brute-force cosine ranking (Module 6 demo)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.2.0",
    "python-dotenv>=1.0.0",
]
