[project]
name = "image-generation"
version = "0.1.0"
description = "Text-to-image generation with the OpenAI Images API (Module 6 demo)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.2.0",
    "python-dotenv>=1.0.0",
]
